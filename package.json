{
  "name": "serverless-plugin-log-class",
  "version": "1.0.0",
  "description": "Serverless Framework plugin to set Cloudwatch log class",
  "main": "index.js",
  "scripts": {
    "lint": "biome lint",
    "format": "biome format",
    "biome": "biome lint --write && biome format --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "serverless": ">=1.70.0"
  },
  "engines": {
    "node": ">=14"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/qub10/serverless-plugin-log-class.git"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2"
  }
}
